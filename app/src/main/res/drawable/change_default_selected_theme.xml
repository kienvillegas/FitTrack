<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <item android:drawable="@drawable/change_theme_default"/>

    <!-- Centered image -->
    <item android:gravity="center">
        <bitmap
            android:src="@drawable/theme_selected_icon"
            android:gravity="center"/>
    </item>
</layer-list>
